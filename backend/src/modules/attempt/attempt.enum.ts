export enum Status {
  SUBMIT = 'submitted',
  AUTOSUBMIT = 'auto-submitted',
  PROGRESS = 'in-progress',
  CALCULATING = 'calculating',
}

export enum AnswerStatus {
  CORRECT = 'correct',
  INCORRECT = 'incorrect',
}
